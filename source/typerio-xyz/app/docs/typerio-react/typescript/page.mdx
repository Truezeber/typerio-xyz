# TypeScript

Typerio-react is primarly built using typescript, which means that you can import types definition and use it for type safety.

```ts
import { TyperioInput, TyperioConfig } from "typerio-react/dist/Typerio";

const inputArray: TyperioInput[] = [/*...*/]
const configurationObject: TyperioConfig = {/*...*/};
```