# HTML quoting in examples

When embedding HTML snippets in docs:

- Prefer fenced code blocks with a language tag
- Quote attributes consistently
- Preview on GitHub before merging

This avoids nested tag rendering glitches in Markdown.
